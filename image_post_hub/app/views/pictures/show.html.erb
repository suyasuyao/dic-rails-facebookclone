<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @picture.title if !@picture.title.nil? %>
</p>

<p>
  <strong>Content:</strong>
  <%= @picture.content if !@picture.content.nil? %>
</p>

<p>
  <strong>PictureFile:</strong>
  <%= image_tag @picture.picture_file.url if !@picture.picture_file.url.nil?  %>
</p>

<p>
  <strong>User:</strong>
  <%= @picture.user.name if !@picture.user.name.nil? %>
</p>

<%= link_to 'Edit', edit_picture_path(@picture) %> |
<%= link_to 'Back', pictures_path %>